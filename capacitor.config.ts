import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.drukzakar.btn',
  appName: 'drukzakar',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
